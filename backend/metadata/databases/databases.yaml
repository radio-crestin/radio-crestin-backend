- name: radiocrestin
  kind: postgres
  configuration:
    connection_info:
      database_url:
        from_env: HASURA_GRAPHQL_DB_URL_FROM_ENV
      isolation_level: read-committed
      use_prepared_statements: false
  tables: "!include radiocrestin/tables/tables.yaml"
