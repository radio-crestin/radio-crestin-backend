table:
  name: songs
  schema: public
array_relationships:
- name: station_now_playings
  using:
    foreign_key_constraint_on:
      column: song_id
      table:
        name: stations_now_playing
        schema: public
- name: stations_now_playings
  using:
    foreign_key_constraint_on:
      column: song_id
      table:
        name: stations_now_playing
        schema: public
select_permissions:
- permission:
    columns:
    - artist
    - id
    - name
    filter: {}
    limit: 1000
  role: public
